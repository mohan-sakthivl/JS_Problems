// 7. Sum of all numbers using a for loop

let numbers = [5, 8, 2, 10, 3];
let sum = 0;

for (let i = 0; i < numbers.length; i++) {
    sum += numbers[i];
}

console.log(`Sum of all numbers: ${sum}`);
